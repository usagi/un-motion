# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.65"
name = "ccap-rs"
version = "1.6.0"
authors = ["wysaid <this@wysaid.org>"]
build = "build.rs"
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Rust bindings for ccap — high-performance, cross-platform webcam/camera capture with hardware-accelerated pixel format conversion (DirectShow/AVFoundation/V4L2), including common RGB/YUV workflows and video file input/playback support"
homepage = "https://ccap.work"
documentation = "https://docs.rs/ccap-rs"
readme = "README.md"
keywords = [
    "camera",
    "webcam",
    "capture",
    "video",
    "ccap",
]
categories = [
    "multimedia::video",
    "api-bindings",
]
license = "MIT"
repository = "https://github.com/wysaid/CameraCapture"

[package.metadata.docs.rs]
no-default-features = true
features = ["build-source"]
targets = ["x86_64-unknown-linux-gnu"]

[features]
build-source = []
default = ["build-source"]
static-link = []

[lib]
name = "ccap"
path = "src/lib.rs"

[[example]]
name = "capture_callback"
path = "examples/capture_callback.rs"

[[example]]
name = "capture_grab"
path = "examples/capture_grab.rs"

[[example]]
name = "minimal_example"
path = "examples/minimal_example.rs"

[[example]]
name = "print_camera"
path = "examples/print_camera.rs"

[[test]]
name = "integration_tests"
path = "tests/integration_tests.rs"

[dependencies.thiserror]
version = "1.0"

[build-dependencies.bindgen]
version = "0.68"

[build-dependencies.cc]
version = "1.0"
